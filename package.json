{
  "name": "sakuya",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "--db"   : "-- DB Script ---------------------------------------",
    "startdb": "docker-compose -f docker/postgresql.yml --env-file docker/.env up -d",
    "stopdb" : "docker-compose -f docker/postgresql.yml --env-file docker/.env down",
    "logindb": "docker exec -it postgresql bash",
    "cleandb": "rm -rf docker/pgsql-data",
    "--app"  : "-- App Script --------------------------------------",
    "dev"    : "next dev",
    "build"  : "next build",
    "start"  : "next start",
    "clean"  : "rm -rf .next",
    "--end"  : "----------------------------------------------------"
  },
  "dependencies": {
    "@types/pg-promise": "^5.4.3",
    "next": "10.0.9",
    "pg-promise": "^10.9.5",
    "react": "17.0.1",
    "react-dom": "17.0.1"
  },
  "devDependencies": {
    "@types/node": "^14.14.35",
    "@types/react": "^17.0.3",
    "@types/react-dom": "^17.0.2",
    "typescript": "^4.2.3"
  }
}
